{
  "ignorePatterns": [
    { "pattern": "^http://localhost" },
    { "pattern": "^https://joyvision.ru" }
  ],
  "timeout": "20s",
  "retryOn429": true,
  "retryCount": 3,
  "fallbackRetryDelay": "30s",
  "aliveStatusCodes": [200, 206, 403, 429]
}
